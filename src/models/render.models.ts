export interface StyleConfig {
  background: string;
  highlight: string;
  cellSize: number;
}

export const defaultStyle: StyleConfig = {
  background: '#fff',
  highlight: 'yellow',
  cellSize: 20,
};